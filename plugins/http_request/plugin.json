{
  "id": "http_request",
  "name": "HTTP Request Plugin",
  "version": "1.0.1",
  "description": "Adiciona blocos HTTP GET e POST para requisições web",
  "blocks": [
    {
      "type": "http_get",
      "name": "HTTP GET",
      "icon": "http",
      "color": "#44aaff",
      "fields": [
        {
          "name": "url",
          "label": "URL",
          "type": "text",
          "default": "https://jsonplaceholder.typicode.com/posts/1",
          "required": true,
          "span": "full"
        },
        {
          "name": "target",
          "label": "Armazenar resultado em",
          "type": "text",
          "default": "response",
          "required": true
        },
        {
          "name": "headers",
          "label": "Headers (JSON)",
          "type": "textarea",
          "default": "{}",
          "placeholder": "{\"Authorization\": \"Bearer token\"}"
        }
      ],
      "allowChildren": false,
      "exportCode": "`(async () => { window.${b.getValue('target')} = await fetch(${b.getValue('url')}, { headers: ${b.getValue('headers')} }).then(r => r.json()); })();`"
    },
    {
      "type": "http_post",
      "name": "HTTP POST",
      "icon": "http",
      "color": "#ffaa44",
      "fields": [
        {
          "name": "url",
          "label": "URL",
          "type": "text",
          "default": "https://jsonplaceholder.typicode.com/posts",
          "required": true,
          "span": "full"
        },
        {
          "name": "body",
          "label": "Body (JSON)",
          "type": "textarea",
          "default": "{\"title\": \"Teste\", \"body\": \"Conteúdo\", \"userId\": 1}",
          "placeholder": "{ \"key\": \"value\" }",
          "span": "full"
        },
        {
          "name": "target",
          "label": "Armazenar resultado em",
          "type": "text",
          "default": "response"
        },
        {
          "name": "headers",
          "label": "Headers (JSON)",
          "type": "textarea",
          "default": "{\"Content-Type\": \"application/json\"}",
          "placeholder": "{\"Content-Type\": \"application/json\"}"
        }
      ],
      "allowChildren": false,
      "exportCode": "`(async () => { window.${b.getValue('target')} = await fetch(${b.getValue('url')}, { method: 'POST', headers: ${b.getValue('headers')}, body: JSON.stringify(${b.getValue('body')}) }).then(r => r.json()); })();`"
    }
  ]
}